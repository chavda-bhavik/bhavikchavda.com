import React from 'react';

import Layout from '@/components/Layout';

interface ProjectsProps {}

const Projects = ({}: ProjectsProps) => {
    return <Layout title="Bhavik Chavda | Project"></Layout>;
};

export default Projects;
