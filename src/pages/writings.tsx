import React from 'react';

import Layout from '@/components/Layout';

interface WritingsProps {}

const Writings = ({}: WritingsProps) => {
    return <Layout title="Bhavik Chavda | Writings"></Layout>;
};

export default Writings;
